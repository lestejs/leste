import { classes } from './classes'
import { native, listeners, general } from './native'
import { iterate, simple, advance, inure, integrate, component } from './component'

const property = { classes, native, listeners, general, iterate, simple, advance, inure, integrate, component }
export { property }